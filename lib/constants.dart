import 'package:flutter/material.dart';

const kRowContainerPadding = EdgeInsets.symmetric(vertical: 8.0, horizontal: 16.0);
const kColorBlue = Color(0xFF0073b1);
final kColorGrey = Colors.black.withOpacity(0.6);
const kColor = AppColors;

class AppColors {
  AppColors._();

  static const blue = Color(0xFF0073b1);
}